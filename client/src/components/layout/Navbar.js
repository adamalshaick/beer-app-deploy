import React, { Component } from "react";
import { StyledNavbar } from "../common/styles/StyledNavbar";

export class Navbar extends Component {
  render() {
    return <StyledNavbar>Navbar</StyledNavbar>;
  }
}

export default Navbar;
